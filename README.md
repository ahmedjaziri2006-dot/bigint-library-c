# bigint-library-c
EN: C implementation of arbitrary-precision integers (BigInt) with addition, subtraction, comparison, multiplication, modulo, and modular exponentiation (RSA basics).  FR:  Implémentation en C d'entiers en précision arbitraire (BigInt) avec addition, soustraction, comparaison, multiplication naïve, modulo et exponentiation modulaire (bases du RSA).
